import "./style.css";

const WhyUsCard = (props) => {
  return (
    <div>
      <h1>{props.data.title}</h1>
      {/* <div>{data.title}</div>
      <p>{data.desc}</p> */}
    </div>
  );
};

export default WhyUsCard;
